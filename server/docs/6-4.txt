添加账号

1.客户端通过点击事件提交表单的数据
2.服务端收到数据以后：
    需要对数据进行验证，添加账号表单验证模型
    验证通过返回通过的接口，并且需要把数据保存数据库中去
        {
            'code':1,
            'msg':'成功'
        }
    验证失败返回失败的接口，客户端要提示报错信息
        {
            'code':0,
            'msg':'失败',
            'data':xxx
        }

配置信息加两个参数：
    xsrf_cookies=True
    cookie_secret='fdf6a806164946fb944bcef6c7083fe2'
加一个xsrf跨站伪装登录保护
防止别人跨域请求

模板需要加入：{% raw xsrf_form_html() %}
